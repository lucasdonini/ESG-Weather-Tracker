import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/ESG-Weather-Tracker/' // Github repository name to be able to deploy on github pages
})
